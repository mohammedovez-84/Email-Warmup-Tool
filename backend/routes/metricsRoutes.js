const express = require('express');
const router = express.Router();
// const metricsController = require('../controllers/metricsController');

// // Get all metrics for a user
// router.get('/user/:userId', metricsController.getAllMetrics);

// // Get metrics for a specific sender (within a user’s account)
// router.get('/user/:userId/sender/:senderEmail', metricsController.getMetricsBySender);


// router.get('/user/:userId/summary', metricsController.getPerSenderSummary);

// // ✅ New route for global summary
// router.get('/user/:userId/total-summary', metricsController.getTotalSenderSummaryController);

module.exports = router;




